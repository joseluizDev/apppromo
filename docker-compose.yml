version: "3.8"

services:
  app:
    build: .
    container_name: react-admin-app
    ports:
      - "80:80"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev -- --host
